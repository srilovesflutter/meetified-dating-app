specVersion: "v1"
serviceId: "meetified"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "meetified-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
